# NEP 项目本地启动说明

## 环境要求

| 依赖 | 版本 | 路径 |
|------|------|------|
| Java (JDK) | 17 | `C:\Program Files\Java\jdk-17` |
| MySQL | 8.x | 本地服务，端口 3306 |
| Node.js | ≥22 | `D:\nodejs`（默认 PATH 中是 v18，需手动切换） |

## 1. 初始化数据库

确保 MySQL 已运行，数据库 `nep` 已创建并导入数据：

```bash
mysql -u root -p123456 -e "SHOW DATABASES LIKE 'nep';"
```

如果数据库不存在，执行初始化脚本：

```bash
mysql -u root -p123456 < nep-backend/src/main/resources/sql/init.sql
```

## 2. 启动后端（Spring Boot 8080）

打开一个新终端：

```cmd
cd C:\Users\Camellia\Desktop\东软实训\neuesoft\DNUI-project-1\nep-backend

set JAVA_HOME=C:\Program Files\Java\jdk-17

mvn spring-boot:run
```

启动成功后显示：`Started NepApplication in X seconds`

**默认账号：** `admin` / `123`

## 3. 启动管理端前端（Vue 3 8083）

打开另一个终端：

```cmd
cd C:\Users\Camellia\Desktop\东软实训\neuesoft\DNUI-project-1\nep-frontend-nepm

:: 切换到 Node 22（否则 Vite 8 会报错）
set PATH=D:\nodejs;%PATH%

:: 首次运行需安装依赖
npm install

:: 启动
npm run dev
```

启动成功后显示：`Local: http://localhost:8083/`

## 4. 访问

浏览器打开：**http://localhost:8083**

账号 `admin`，密码 `123`。

## 端口一览

| 服务 | 端口 |
|------|------|
| 后端 Spring Boot | 8080 |
| 管理端 NEPM | 8083 |
| 监督员端 NEPS | 8081（如已构建） |
